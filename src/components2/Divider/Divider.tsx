export type DividerProps = {};

export function Divider(_props: DividerProps) {
  const style = {
    height: '25px',
  };

  return <Wrapper className='Divider-container' style={style}></Wrapper>;
}

export default Divider;
