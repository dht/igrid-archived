import { Wrapper } from './Divider.style';

export type DividerProps = {};

export function Divider(_props: DividerProps) {
  return <Wrapper className='Divider-wrapper' data-testid='Divider-wrapper'></Wrapper>;
}

export default Divider;
